let pyodideService=null;document.addEventListener('DOMContentLoaded',function(){console.log('🚀 Site Petit Prof initialisé avec succès!');initPyodideService();initLazyLoading();const loadTime=performance.now();console.log(`Temps de chargement: ${loadTime}ms`)});function initPyodideService(){pyodideService=new PyodideService();console.log('📦 Service Pyodide initialisé')}function initLazyLoading(){const images=document.querySelectorAll('img[data-src]');console.log(`Lazy loading activé pour ${images.length}images`);const imageObserver=new IntersectionObserver((entries,observer)=>{entries.forEach(entry=>{if(entry.isIntersecting){const img=entry.target;img.src=img.dataset.src;img.classList.remove('lazy');observer.unobserve(img)}})});images.forEach(img=>imageObserver.observe(img))}function showError(message){console.error('Erreur:',message)}function showLoading(message){console.log('Chargement:',message)}function hideLoading(){}window.PetitProf={showError,showLoading,hideLoading,pyodideService:()=>pyodideService};