class SimpleMathSystem{constructor(){this.isReady=true;console.log('🚀 Système mathématique simple initialisé!')}validateExpression(expression){if(!expression||!expression.trim()){return{valid: false,error: 'Expression vide'}}const allowedChars=/^[0-9x+\-*/().,^a-zA-Z\s]+$/;if(!allowedChars.test(expression)){return{valid: false,error: 'Caractères non autorisés'}}return{valid: true}}evaluateFunction(expression,xValue){try{let processed=expression .replace(/\^/g,'**').replace(/sin\(/g,'Math.sin(').replace(/cos\(/g,'Math.cos(').replace(/tan\(/g,'Math.tan(').replace(/ln\(/g,'Math.log(').replace(/log\(/g,'Math.log10(').replace(/exp\(/g,'Math.exp(').replace(/sqrt\(/g,'Math.sqrt(').replace(/abs\(/g,'Math.abs(').replace(/pi/g,'Math.PI').replace(/e/g,'Math.E');const expr=processed.replace(/x/g,`(${xValue})`);return eval(expr)}catch(error){console.error('Erreur d\'évaluation:',error);return NaN}}generatePlotPoints(expression,xMin,xMax,numPoints=1000){const points={x: [],y: []};const step=(xMax-xMin)/numPoints;for(let i=0;i<numPoints;i++){const x=xMin+i*step;const y=this.evaluateFunction(expression,x);if(isFinite(y)){points.x.push(x);points.y.push(y)}}return points}calculateDerivative(expression){return(x)=>{const h=0.0001;const f1=this.evaluateFunction(expression,x+h);const f2=this.evaluateFunction(expression,x-h);return(f1-f2)/(2*h)}}calculateTangent(expression,xPoint){const yPoint=this.evaluateFunction(expression,xPoint);const derivative=this.calculateDerivative(expression);const slope=derivative(xPoint);const intercept=yPoint-slope*xPoint;return{slope: slope,intercept: intercept,point: [xPoint,yPoint],equation: `y=${slope.toFixed(3)}x+${intercept.toFixed(3)}`}}drawGrid(ctx,xMin,xMax,yMin,yMax,width,height){ctx.strokeStyle='#e5e7eb';ctx.lineWidth=1;for(let x=Math.ceil(xMin);x<=Math.floor(xMax);x++){const pixelX=this.xToPixel(x,xMin,xMax,width);ctx.beginPath();ctx.moveTo(pixelX,0);ctx.lineTo(pixelX,height);ctx.stroke()}for(let y=Math.ceil(yMin);y<=Math.floor(yMax);y++){const pixelY=this.yToPixel(y,yMin,yMax,height);ctx.beginPath();ctx.moveTo(0,pixelY);ctx.lineTo(width,pixelY);ctx.stroke()}ctx.strokeStyle='#374151';ctx.lineWidth=2;const zeroY=this.yToPixel(0,yMin,yMax,height);if(zeroY>=0&&zeroY<=height){ctx.beginPath();ctx.moveTo(0,zeroY);ctx.lineTo(width,zeroY);ctx.stroke()}const zeroX=this.xToPixel(0,xMin,xMax,width);if(zeroX>=0&&zeroX<=width){ctx.beginPath();ctx.moveTo(zeroX,0);ctx.lineTo(zeroX,height);ctx.stroke()}}drawFunction(ctx,points,color='#2563eb',lineWidth=2){if(!points||points.x.length===0)return;ctx.strokeStyle=color;ctx.lineWidth=lineWidth;ctx.beginPath();for(let i=0;i<points.x.length;i++){if(i===0){ctx.moveTo(points.x[i],points.y[i])}else{ctx.lineTo(points.x[i],points.y[i])}}ctx.stroke()}xToPixel(x,xMin,xMax,width){return((x-xMin)/(xMax-xMin))*width}yToPixel(y,yMin,yMax,height){return height-((y-yMin)/(yMax-yMin))*height}pixelToX(pixelX,xMin,xMax,width){return xMin+(pixelX/width)*(xMax-xMin)}pixelToY(pixelY,yMin,yMax,height){return yMin+((height-pixelY)/height)*(yMax-yMin)}showError(message){const errorElement=document.getElementById('errorMessage');if(errorElement){errorElement.textContent=message;errorElement.style.display='block'}console.error('Erreur:',message)}hideError(){const errorElement=document.getElementById('errorMessage');if(errorElement){errorElement.style.display='none'}}showLoading(message,canvas=null){if(canvas){const ctx=canvas.getContext('2d');ctx.fillStyle='rgba(255,255,255,0.9)';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#2563eb';ctx.font='16px Arial';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(message,canvas.width/2,canvas.height/2)}}hideLoading(canvas=null){if(canvas){const ctx=canvas.getContext('2d');ctx.clearRect(0,0,canvas.width,canvas.height)}}}window.SimpleMathSystem=SimpleMathSystem;